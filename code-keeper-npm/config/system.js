/**
 * Created by nero on 2017/2/15.
 * dirname of router
 */

let systemconfig = {
  plugin: 'plugin/init/',
  root: './',
  frontdir: 'front/',
  staticdir: 'static/',
  source: 'source/js/'
}

module.exports = systemconfig
